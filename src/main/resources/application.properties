spring.application.name=fixpoint

# H2 Database C# Email Configuration for OTP Verification
# Primary: Gmail SMTP Configuration (reads from .env file)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:tahseennabiha35@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:gfltqeedcqqrqqtb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.debug=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Alternative: Outlook SMTP (uncomment these and comment Gmail above to use)
# spring.mail.host=smtp-mail.outlook.com
# spring.mail.port=587
# spring.mail.username=${EMAIL_USERNAME:your_email@outlook.com}
# spring.mail.password=${EMAIL_PASSWORD:your_password}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.ssl.trust=smtp-mail.outlook.com

# Option 2: Alternative - Outlook/Hotmail (uncomment to use)
# spring.mail.host=smtp-mail.outlook.com
# spring.mail.port=587
# spring.mail.username=${EMAIL_USERNAME:your_email@outlook.com}
# spring.mail.password=${EMAIL_PASSWORD:your_password}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# Option 3: Development Mode - Use MailHog (uncomment to use)
# spring.mail.host=localhost
# spring.mail.port=1025
# spring.mail.username=
# spring.mail.password=

# H2 Database Configuration for Development - File-based for persistence
spring.datasource.url=jdbc:h2:file:./data/fixpoint;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration
jwt.secret=mySecretKeyForFixPointApplicationThatIsLongEnoughForJWTSecurity2024
jwt.expiration=86400000

# Server Configuration
# Server Configuration
server.port=${PORT:8080}

# CORS Configuration
# CORS Configuration
cors.allowed.origins=http://localhost:3000,https://itsamisha.github.io,https://fixpoint-ajtz.onrender.com

# AI Configuration
# Choose AI provider: "gemini", "openai", or "fallback"
ai.provider=gemini

# Google Gemini Configuration (Recommended - Free tier available)
# Get your API key from: https://makersuite.google.com/app/apikey
gemini.api.key=${GEMINI_API_KEY:AIzaSyAmpBVl_Bp20E7ojhzrbsY2nyVH1TqYyow}

# OpenAI Configuration (Backup)
# Get your API key from: https://platform.openai.com/api-keys
openai.api.key=${OPENAI_API_KEY:}

# Vertex AI Configuration (if using Google Vertex AI)
# Get your project ID from: https://console.cloud.google.com/
vertexai.project.id=${VERTEX_PROJECT_ID:your_vertex_project_id_here}
vertexai.location=${VERTEX_LOCATION:us-central1}

# Google Cloud Speech-to-Text Configuration
google.cloud.project-id=your-google-cloud-project-id
google.cloud.speech.enabled=false
google.cloud.speech.credentials-file=classpath:google-credentials.json

# Application Email Settings
app.name=FixPoint
app.email.from=${EMAIL_FROM:FixPoint <noreply@fixpoint.com>}

# OTP Configuration
app.otp.expiry-minutes=10
app.otp.max-attempts=3
