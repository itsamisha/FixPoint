# Production Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:h2:file:./data/fixpoint;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.driver-class-name=${DB_DRIVER:org.h2.Driver}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
# Note: Removed hibernate.dialect as H2Dialect is auto-detected

# Server Configuration
server.port=${PORT:8080}

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:http://localhost:3000,https://itsamisha.github.io,https://fixpoint-ajtz.onrender.com}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:tahseennabiha35@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:gfltqeedcqqrqqtb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKeyForFixPointApplicationThatIsLongEnoughForJWTSecurity2024}
jwt.expiration=86400000

# AI Configuration
ai.provider=gemini
gemini.api.key=${GEMINI_API_KEY:AIzaSyAmpBVl_Bp20E7ojhzrbsY2nyVH1TqYyow}

# Application Settings
app.name=FixPoint
app.email.from=${EMAIL_FROM:FixPoint <noreply@fixpoint.com>}
app.otp.expiry-minutes=10
app.otp.max-attempts=3

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.ambiguous.fixpoint=INFO
logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR
